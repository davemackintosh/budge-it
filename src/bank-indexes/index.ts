import { halifax } from "@banks/halifax"
import { natwest } from "@banks/natwest"
import { hsbc } from "@banks/hsbc"
import { Indexers } from "@budge-types/base"

const banks: Indexers = {
  halifax,
  natwest,
  hsbc,
}

export default banks
